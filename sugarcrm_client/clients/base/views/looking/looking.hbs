<span id="whos-looking" style="letter-spacing: 0em;" class="dropdown dropup">
  <a data-hover="dropdown" data-toggle="dropdown" role="button" class="btn btn-success" href="#">{{count}}</a>
  <div class="dropdown-menu pull-right scroll">
      <ul role="menu" aria-labelledby="dLabel">
          {{#each users}}
            <li><a href="" class="ellipsis_inline">{{name}}</a></li>
          {{/each}}
      </ul>
  </div>
</span>
